class BackLightEffect {
    constructor() {
        let backLightEffect = document.createElement("div");
        backLightEffect.setAttribute("class", "light");
        document.body.appendChild(backLightEffect);
    }
}